# nye-site-play

Personal homepage (v1, home only) for Nye Warburton. Fast Astro static site. Vercel playground — root hosting, base `/`.

## Ownership

| Role | Who |
| --- | --- |
| Owner | Anvil (Forge family) |
| Lead | Forge |
| CoS | Nyborg |
| Merge | Nye only |

This is personal brand. Do not nest STEC Satellite Lab site content here.

## Local

Requires Node >= 22.

```bash
npm install
npm run dev
npm run build
npm run preview
```

Edit copy in `content/`. Hero, ethos, work links, and meta live in JSON. Background bio is `content/background.md`.
